<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.appspot.c_three_games_test.war"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateNewGame"
            android:label="@string/title_activity_create_new_game">
        </activity>
        <activity
            android:name=".JoinExistingGame"
            android:label="@string/title_activity_join_existing_game">
        </activity>
        <activity
            android:name=".ContinueGame"
            android:label="@string/title_activity_continue_game">
        </activity>
    </application>

</manifest>
